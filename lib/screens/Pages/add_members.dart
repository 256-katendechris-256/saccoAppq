import 'package:flutter/material.dart';

class add_memberPage extends StatelessWidget {
  const add_memberPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        'add member Page',
        style: TextStyle(fontSize: 24),
      ),
    );
  }
}
