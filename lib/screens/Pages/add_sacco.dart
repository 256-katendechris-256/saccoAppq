import 'package:flutter/material.dart';

class add_saccoPage extends StatelessWidget {
  const add_saccoPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        'add sacco Screen',
        style: TextStyle(fontSize: 24),
      ),
    );
  }
}
