import 'package:flutter/material.dart';

class take_surveypage extends StatelessWidget {
  const take_surveypage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        'survey Screen',
        style: TextStyle(fontSize: 24),
      ),
    );
  }
}
